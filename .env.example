NEXT_PUBLIC_SUPABASE_URL=https://tbumrkmjeglwdgkcwevf.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Access rule: email must end with @<domain> AND its local part
# (before the @) must start with one of the comma-separated prefixes.
# Both have sane defaults in lib/whitelist.ts if left unset.
ALLOWED_EMAIL_DOMAIN=students.csdmi.org
ALLOWED_ID_PREFIXES=2001,2002
